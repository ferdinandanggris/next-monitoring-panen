'use client';
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette : {
    mode : "light",
    background : {
      default : "#f5f5f5"
    }
  }
});

export default theme;